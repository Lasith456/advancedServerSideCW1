version: '3.8'

services:
#   mysql:
#     image: mysql:8.0
#     container_name: doctor_db
#     environment:
#       MYSQL_ROOT_PASSWORD: trytocatchme  
#       MYSQL_DATABASE: doctor_app
#       MYSQL_USER: badguy                            
#       MYSQL_PASSWORD: trytocatchme        
#     ports:
#       - "3307:3306"                                   
#     volumes:
#       - mysql_data:/var/lib/mysql
#     networks:
#       - doctor_network
#     command: --default-authentication-plugin=mysql_native_password

  server:
    build: ./server
    container_name: server-container
    ports:
      - "3001:3001"                                                                        
    volumes:
      - ./server:/app
      - /server/node_modules:/app/node_modules        
    # environment:
    #   - DB_HOST=mysql                                 
    #   - DB_PORT=3306                                  
    #   - DB_USER=badguy
    #   - DB_NAME=doctor_app
    networks:
      - doctor_network

volumes:
  mysql_data:

networks:
  doctor_network:                                    